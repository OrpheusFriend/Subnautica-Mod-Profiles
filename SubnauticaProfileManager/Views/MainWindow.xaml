<Window x:Class="SubnauticaProfileManager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:SubnauticaProfileManager.ViewModels"
        mc:Ignorable="d"
        Title="Subnautica Profile Manager" Height="650" Width="1000">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Padding="8" BorderThickness="1" BorderBrush="#3A3A3A" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Game Path:" VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBox Grid.Column="1" Text="{Binding GamePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                <Button Grid.Column="2" Content="Detect" Command="{Binding DetectGamePathCommand}" Margin="0,0,8,0" />
                <Button Grid.Column="3" Content="Browse" Command="{Binding BrowseGamePathCommand}" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderThickness="1" BorderBrush="#3A3A3A" CornerRadius="6" Padding="8">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Profiles" FontWeight="Bold" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" />
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <Button Content="Add" Command="{Binding AddProfileCommand}" Margin="0,0,8,0" />
                        <Button Content="Delete" Command="{Binding DeleteProfileCommand}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Column="2" BorderThickness="1" BorderBrush="#3A3A3A" CornerRadius="6" Padding="8">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,8">
                        <TextBlock Text="Mods" FontWeight="Bold" VerticalAlignment="Center" />
                        <Button Content="Refresh" Command="{Binding RefreshModsCommand}" Margin="8,0,0,0" />
                        <Button Content="Save Profile" Command="{Binding SaveProfileCommand}" Margin="8,0,0,0" />
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Mods}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsEnabled, Mode=TwoWay}" Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Content="Switch Profile" Command="{Binding SwitchProfileCommand}" Margin="0,0,8,0" Width="140" />
            <Button Content="Launch Game" Command="{Binding LaunchGameCommand}" Width="120" />
        </StackPanel>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="#CCCCCC" />
    </Grid>
</Window>
